package io.github.dfa1.refinedtypes;

/**
 * Marker interface for refined types with a bounded value space.
 * Implementations may be cached to avoid repeated validation cost.
 */
public interface LowCardinality {
}
